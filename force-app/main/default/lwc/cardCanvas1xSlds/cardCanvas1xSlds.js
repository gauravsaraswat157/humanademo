import { LightningElement } from "lwc";
import { BaseLayout } from "vlocity_ins/baseLayout";
import temp from "./cardCanvas1xSlds.html";
export default class cardCanvas1x extends BaseLayout(LightningElement) {
  render() {
    return temp;
  }
}